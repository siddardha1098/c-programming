#include <stdio.h>
int main() {
    double n, m, product;
    printf("Enter two numbers: ");
    scanf("%lf %lf", &n, &m);  
    product = a * 
    printf("Product = %.2lf", product);
    return 0;
}
